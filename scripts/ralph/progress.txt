# Ralph Progress Log
Started: Mon Mar 16 12:12:17 UTC 2026
---

## Codebase Patterns
- Monorepo uses npm workspaces with packages/* and apps/* layout
- Root tsconfig.json is the base; each package extends it with its own rootDir/outDir
- packages/core uses ESM (`"type": "module"`) with NodeNext module resolution
- apps/web uses Next.js App Router with bundler module resolution (different from packages)
- @ingot/core is referenced as `"@ingot/core": "*"` in dependent package.json files
- Run `npm run typecheck` from root to check all workspaces
- apps/web global styles use CSS vars (--bg, --border, --text, --text-muted, --text-dim) defined in globals.css; use inline `style=` for these vars in components rather than Tailwind classes since Tailwind doesn't know the CSS vars
- apps/web uses JetBrains_Mono from `next/font/google`, exposed as `--font-mono` CSS variable and applied to html element
---

## 2026-03-16 - US-001
- Created monorepo structure with npm workspaces
- packages/cli: empty TypeScript package with bin entry
- packages/core: empty TypeScript package (ESM)
- apps/web: Next.js App Router + TypeScript + Tailwind (manually scaffolded)
- Root tsconfig.json base; each workspace extends it
- Files changed: package.json, tsconfig.json, packages/cli/*, packages/core/*, apps/web/*
- **Learnings for future iterations:**
  - apps/web tsconfig does NOT extend root tsconfig — Next.js needs its own compiler options (bundler moduleResolution, noEmit, jsx:preserve)
  - Build works with `npm run build --workspaces --if-present`
  - apps/web build requires `next build` which requires full Next.js setup; typecheck via `tsc --noEmit` is sufficient for CI
---

## 2026-03-16 - US-003
- Created packages/core/src/packageManager.ts with detectPackageManager() function
- Lockfile detection: bun.lockb → bun, pnpm-lock.yaml → pnpm, package-lock.json → npm
- PATH fallback using `which` via execSync, defaults to npm
- Exported detectPackageManager and PackageManager type from packages/core/src/index.ts
- Files changed: packages/core/src/packageManager.ts, packages/core/src/index.ts
- **Learnings for future iterations:**
  - `execSync` with `stdio: 'ignore'` is the cleanest way to check binary availability
  - The `cwd` param defaults to `process.cwd()` for testability
  - ESM `.js` extension required in imports (as per existing pattern)
---

## 2026-03-16 - US-004
- Created packages/core/src/orchestrator.ts with OrchestratorStep type and getSteps()
- OrchestratorStep has: label (string), command (string | function), envVars (string[]), postRun? (function)
- getSteps() returns framework step first; nextjs uses --yes flag to suppress prompts; tanstack-start uses gitpkg; remix uses --yes flag
- Exported getSteps and OrchestratorStep from packages/core/src/index.ts
- Files changed: packages/core/src/orchestrator.ts, packages/core/src/index.ts
- **Learnings for future iterations:**
  - getSteps() is designed to accumulate steps — future stories (US-005, 006, 007) should add to the returned array after the framework step
  - OrchestratorStep.command accepts string or async function for flexibility when a step can't be expressed as a shell command
  - The `--yes` flag on create-next-app suppresses all interactive prompts
---

## 2026-03-16 - US-005
- Added getDbOrmStep() to packages/core/src/orchestrator.ts
- Drizzle step: installs drizzle-orm + drizzle-kit, runs drizzle-kit generate with correct --dialect flag (postgresql/mysql/sqlite)
- Prisma step: installs prisma + @prisma/client, runs prisma init with --datasource-provider flag
- Both steps include DATABASE_URL in envVars
- getSteps() now returns [frameworkStep, dbOrmStep]
- Files changed: packages/core/src/orchestrator.ts
- **Learnings for future iterations:**
  - Drizzle uses dialect names "postgresql"/"mysql"/"sqlite" (not "postgres") for its CLI flags
  - getSteps() accumulates steps in order — US-006 auth step should be appended after dbOrmStep
  - Prisma init creates schema.prisma automatically; drizzle-kit generate needs a config file in practice but the command is correct for scaffolding
---

## 2026-03-16 - US-006
- Added getAuthStep() to packages/core/src/orchestrator.ts
- Clerk step: installs @clerk/nextjs (or @clerk/remix for remix), adds NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY + CLERK_SECRET_KEY envVars
- Better Auth step: installs better-auth, adds BETTER_AUTH_SECRET + BETTER_AUTH_URL envVars
- NextAuth step: installs next-auth, adds NEXTAUTH_SECRET + NEXTAUTH_URL envVars
- auth: 'none' returns null and is skipped in getSteps()
- Added docUrl field to OrchestratorStep type (optional)
- getSteps() now returns [frameworkStep, dbOrmStep, authStep?]
- Files changed: packages/core/src/orchestrator.ts
- **Learnings for future iterations:**
  - OrchestratorStep.docUrl is an optional field — only auth steps use it currently
  - getAuthStep returns null for 'none' — use null-check pattern before pushing to steps array
  - Clerk package varies by framework (@clerk/nextjs vs @clerk/remix); tanstack-start uses @clerk/tanstack-start pattern
---

## 2026-03-16 - US-007
- Added getShadcnStep() to orchestrator.ts: runs `npx shadcn@latest init --theme <theme> --yes`
- Added getEnvExampleStep() to orchestrator.ts: async function that aggregates all envVars from preceding steps and writes .env.example with commented key=value pairs
- getSteps() now returns [frameworkStep, dbOrmStep, authStep?, shadcnStep, envExampleStep]
- Files changed: packages/core/src/orchestrator.ts
- **Learnings for future iterations:**
  - .env.example step uses a dynamic import of `node:fs` inside the async command function to stay compatible with ESM
  - getEnvExampleStep receives the already-built steps array (before itself) to collect envVars — must be called after all other steps are pushed
  - Shadcn step passes theme via `--theme <theme>` flag with `--yes` to suppress prompts
---

## 2026-03-16 - US-008
- Implemented interactive CLI in packages/cli/src/index.ts using @clack/prompts
- Prompts cover all fields: project name → framework → DB provider → ORM → auth → UI theme → deployment
- Validates assembled config with validateConfig() before execution
- Each step runs with a Clack spinner; on failure prints step label + command + exits with code 1
- On success, prints next-steps outro with cd/cp/.env instructions
- Built @ingot/core first (dist/) so CLI package can resolve types
- Files changed: packages/cli/src/index.ts, packages/cli/package.json, package-lock.json
- **Learnings for future iterations:**
  - @ingot/core exports from dist/; must `npm run build --workspace=packages/core` before typechecking CLI
  - @clack/prompts select() generic: `select<T>({ options: Array<{value: T}> })` — pass the enum type for type safety
  - text() validate callback receives `string | undefined` — use `(v ?? '')` to avoid TS18048
  - isCancel(result) pattern with process.exit(0) is the standard Clack cancellation guard
---

## 2026-03-16 - US-009
- Refactored packages/cli/src/index.ts to support non-interactive modes
- Added parseArgs() to parse --config, --framework, --db <provider>/<orm>, --auth, --ui <library>/<theme>, --deployment, and positional name
- --config mode: reads JSON file, validates with validateConfig(), runs without prompts
- Fully inline flags mode: if all fields provided via flags, validates and runs without prompts
- Partial flags mode: pre-fills provided values, prompts only for missing fields
- Invalid flags/config produce clear error message and exit(1) before any scaffolding
- Extracted runScaffold(config) function — shared execution path for all modes
- Files changed: packages/cli/src/index.ts
- **Learnings for future iterations:**
  - `--db postgres/drizzle` and `--ui shadcn/zinc` use slash-separated compound flags
  - The `init` subcommand is stripped from argv before flag parsing
  - Validation happens before any scaffold steps in all modes — use try/catch around validateConfig()
  - runScaffold() is the single execution path — interactive, config file, and flags all converge there
---

## 2026-03-16 - US-002
- Created packages/core/src/schema.ts with full Zod schema for IngotConfig
- Exported IngotConfig type and validateConfig() from packages/core/src/index.ts
- Installed zod as a dependency in packages/core
- Files changed: packages/core/src/schema.ts, packages/core/src/index.ts, packages/core/package.json, package-lock.json
- **Learnings for future iterations:**
  - ESM packages (NodeNext resolution) require `.js` extensions in imports even for `.ts` source files
  - Zod's .parse() throws ZodError with descriptive messages automatically — no custom error wrapping needed
  - Export `type` separately from value exports to keep type-only re-exports explicit
---

## 2026-03-16 - US-011
- Created apps/web/src/components/ConfiguratorForm.tsx as a "use client" component
- Multi-step form with 7 steps: project name (text input) → framework → DB provider → ORM → auth → UI theme → deployment
- Each non-text step renders selectable cards with border highlight on selection
- Step indicator: animated dot row (active dot expands to 24px wide) with "N / 7" counter
- Dot buttons allow navigating back to previously completed steps
- Next/Back buttons; final step shows "generate" button
- Next is disabled and visually muted until a selection is made for the current step
- UI theme step shows colored swatches (no description, smaller grid columns)
- Exported FormConfig type and onConfigChange?: callback for US-012 to wire in live preview
- Updated apps/web/src/app/page.tsx to import and render <ConfiguratorForm />
- Files changed: apps/web/src/components/ConfiguratorForm.tsx, apps/web/src/app/page.tsx
- **Learnings for future iterations:**
  - ConfiguratorForm exposes `onConfigChange?: (config: FormConfig) => void` — US-012 should make page.tsx a client component, lift state, and pass this callback to wire the preview panel
  - FormConfig and STEPS are defined in ConfiguratorForm.tsx — import from there for the preview panel
  - Browser verification not done (no dev-browser tool); manual check needed
---

## 2026-03-16 - US-010
- Updated globals.css: dark mode CSS vars (--bg, --bg-subtle, --border, --text, --text-muted, --text-dim), monospace body font, no gradients, structural TanStack aesthetic
- Updated layout.tsx: JetBrains_Mono from next/font/google, dark html with --font-mono variable
- Updated page.tsx (/ route): header with nav, 2-column layout (form area left, preview panel right with placeholders for US-011/012)
- Created apps/web/src/app/docs/page.tsx: header, sidebar nav, docs content with all 6 sections (what is ingot, quick start, CLI ref, schema, supported pieces table, FAQ)
- Files changed: apps/web/src/app/globals.css, apps/web/src/app/layout.tsx, apps/web/src/app/page.tsx, apps/web/src/app/docs/page.tsx
- **Learnings for future iterations:**
  - Use inline `style=` props for CSS custom vars (--bg etc.) since Tailwind cannot resolve them
  - `next/font/google` with `JetBrains_Mono` works fine in Next.js 14; import name uses underscores for spaces
  - docs/page.tsx is a Server Component — no "use client" needed for static content
  - Browser verification was not done (no dev-browser tool configured); manual browser check needed
---
